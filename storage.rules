rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Partner files: authenticated users belonging to that partner can read/write
    match /partners/{partnerId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
